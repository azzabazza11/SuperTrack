<set xmlns:android="http://schemas.android.com/apk/res/android">
    <scale
        android:interpolator="@android:anim/cycle_interpolator"
        android:fromXScale="5.0"
        android:toXScale="2.0"
        android:fromYScale="5.0"
        android:toYScale="2.0"
        android:pivotX="30%"
        android:pivotY="20%"
        android:duration="10000" /> <!-- Reduce duration for faster animation -->
    <scale
        android:interpolator="@android:anim/cycle_interpolator"
        android:fromXScale="2.0"
        android:toXScale="5.0"
        android:fromYScale="2.0"
        android:toYScale="5.0"
        android:pivotX="10%"
        android:pivotY="10%"
        android:duration="22000"
        /> <!-- Reduce duration for faster animation -->
   <!-- <scale
        android:interpolator="@android:anim/cycle_interpolator"
        android:fromXScale="1.0"
        android:toXScale="0.5"
        android:fromYScale="1.0"
        android:toYScale="0.5"
        android:pivotX="50%"
        android:pivotY="50%"
        android:duration="1000" /> &lt;!&ndash; Reduce duration for faster animation &ndash;&gt;

    <scale
    android:interpolator="@android:anim/cycle_interpolator"
    android:fromXScale="0.5"
    android:toXScale="1.0"
    android:fromYScale="0.5"
    android:toYScale="1.0"
    android:pivotX="50%"
    android:pivotY="50%"

    android:duration="1000" />  Reduce duration for faster animation -->

</set>
